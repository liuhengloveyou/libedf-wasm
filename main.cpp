#include <stdio.h>
#include <stdlib.h>

#include "edflib.h"
#include "edf.h"


// Main code
int main(int, char**)
{ 
    //const char *fn = "./data/chb01_01.edf";
    // int rst = edf_open(fn);
    // if (rst) {
    //     fprintf(stderr, "edf_open err: %d\n", rst);
    //     return -1;
    // }
    // fprintf(stdout, "edf_open ok\n");

    // edf_read_datarecord(1, 10001);

    return 0;
}